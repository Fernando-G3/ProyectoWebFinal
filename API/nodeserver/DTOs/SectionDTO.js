class SectionDTO {
  constructor(idSaleSection, section, price, idEvent) {
    this.idSaleSection = idSaleSection;
    this.section = section;
    this.price = price;
    this.idEvent = idEvent;
  }
}

module.exports = SectionDTO;
